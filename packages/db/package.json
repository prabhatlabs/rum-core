{
  "name": "@rum-core/db",
  "module": "src/index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:generate:eventdb": "drizzle-kit generate --config drizzle.eventdb.config.ts",
    "db:push": "drizzle-kit push",
    "db:push:eventdb": "drizzle-kit push --config drizzle.eventdb.config.ts",
    "db:migrate": "drizzle-kit migrate",
    "db:migrate:eventdb": "drizzle-kit migrate --config drizzle.eventdb.config.ts",
    "db:studio": "drizzle-kit studio",
    "db:studio:eventdb": "drizzle-kit studio --config drizzle.eventdb.config.ts",
    "db:pull": "drizzle-kit pull"
  },
  "type": "module",
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "@rum-core/shared": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "nanoid": "^5.1.7"
  }
}