{
  "name": "@rum-core/worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.7",
    "@rum-core/db": "workspace:*",
    "@rum-core/shared": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}